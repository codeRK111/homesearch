import { makeStyles } from '@material-ui/core/styles';

export const trimStyle = makeStyles((theme) => ({
	button: {
		border: 'none',
		color: 'blue',
		textDecoration: 'underline',
		cursor: 'pointer',
	}
}))